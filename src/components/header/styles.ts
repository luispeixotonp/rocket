import styled from 'styled-components';

export const Container = styled.div`
  flex: 1;
  padding: 16px;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  background: #fff;
`;
